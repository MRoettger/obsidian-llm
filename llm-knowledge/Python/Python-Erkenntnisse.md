# 🐍 Python-Erkenntnisse

Themen-Überblick zu meinem Python-Wissen, abgeleitet aus den Anki-Decks **Python** und **Python::Fortgeschritten**. Jede Themen-Notiz fasst die Kernaussagen der zugehörigen Karten zusammen und führt am Ende eine Tabelle der erfassten Anki-Karten-IDs (für duplikatsicheres Erweitern).

Quelle der Wahrheit bleibt Anki — diese Notizen sind die durchsuchbare, verlinkbare Zusammenfassung.

---

## Themen

- [[Python - Syntax und Datentypen]] — Slicing, Comprehensions, Unpacking, Falsy-Werte, Sets, Floats, list/tuple
- [[Python - Funktionen und OOP]] — Decorators, Closures, Properties, Deskriptoren, Dataclasses, MRO, super, Dunder-Methoden
- [[Python - Standardbibliothek]] — collections, itertools, functools, pathlib, datetime, json, enum, logging
- [[Python - Async und Performance]] — asyncio, GIL, Threads vs. Prozesse, Concurrency vs. Parallelism, Profiling
- [[Python - Datenstrukturen und Komplexität]] — heapq, Membership-Komplexität (list vs. set)
- [[Python - Data Science (numpy & pandas)]] — loc/iloc, Vektorisierung, apply/map, Views vs. Kopien, SettingWithCopyWarning

---

## Wie diese Notizen gepflegt werden

Die Notizen werden aus Anki erzeugt und sind so angelegt, dass beim erneuten Lauf **nur neue Karten** ergänzt werden (kein doppelter Eintrag). Maßgeblich ist die Spalte **Anki-ID** in der jeweiligen Themen-Notiz: Karten, deren ID dort schon steht, werden übersprungen; neue Karten werden im passenden Thema angehängt.

Auslösen mit z. B.: *„aktualisiere meine Python-Erkenntnisse aus Anki“*.

---
*Stand: Juni 2026 | Quelle: Anki-Decks „Python“ + „Python::Fortgeschritten“ (76 Karten)*
# 🐍 Python-Erkenntnisse

Themen-Überblick zu meinem Python-Wissen, abgeleitet aus den Anki-Decks **Python** und **Python::Fortgeschritten**. Jede Themen-Notiz fasst die Kernaussagen der zugehörigen Karten zusammen und führt am Ende eine Tabelle der erfassten Anki-Karten-IDs (für duplikatsicheres Erweitern).

Quelle der Wahrheit bleibt Anki — diese Notizen sind die durchsuchbare, verlinkbare Zusammenfassung.

---

## Themen

- [[Python - Syntax und Datentypen]] — Slicing, Comprehensions, Unpacking, Falsy-Werte, Sets, Floats, list/tuple
- [[Python - Funktionen und OOP]] — Decorators, Closures, Properties, Deskriptoren, Dataclasses, MRO, super, Dunder-Methoden
- [[Python - Standardbibliothek]] — collections, itertools, functools, pathlib, datetime, json, enum, logging
- [[Python - Async und Performance]] — asyncio, GIL, Threads vs. Prozesse, Concurrency vs. Parallelism, Profiling
- [[Python - Datenstrukturen und Komplexität]] — heapq, Membership-Komplexität (list vs. set)
- [[Python - Data Science (numpy und pandas)]] — loc/iloc, Vektorisierung, apply/map, Views vs. Kopien, SettingWithCopyWarning

---

## Wie diese Notizen gepflegt werden

Die Notizen werden aus Anki erzeugt und sind so angelegt, dass beim erneuten Lauf **nur neue Karten** ergänzt werden (kein doppelter Eintrag). Maßgeblich ist die Spalte **Anki-ID** in der jeweiligen Themen-Notiz: Karten, deren ID dort schon steht, werden übersprungen; neue Karten werden im passenden Thema angehängt.

Auslösen mit z. B.: *„aktualisiere meine Python-Erkenntnisse aus Anki“*.

---
*Stand: Juni 2026 | Quelle: Anki-Decks „Python" + „Python::Fortgeschritten" (76 Karten)*
